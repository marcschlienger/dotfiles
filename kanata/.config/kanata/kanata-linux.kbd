(defcfg
  process-unmapped-keys yes
  linux-dev-names-include (
    "AT Translated Set 2 keyboard"
  )
)

(include common.kbd)

(defsrc
  caps lctl lsft rsft lalt lmet ralt sys rctl
)

(deflayer base
  @os-lctrl @os-lalt @os-lshift @os-rshift @os-lctrl @os-lmeta
  @os-rctrl @os-rmeta ralt
)
